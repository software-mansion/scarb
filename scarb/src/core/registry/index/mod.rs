// FIXME(mkaput): Remove this when we will indeed use this.
#![allow(unused)]

pub use base_url::*;
pub use config::*;
pub use record::*;
pub use template_url::*;

mod base_url;
mod config;
mod record;
mod template_url;
